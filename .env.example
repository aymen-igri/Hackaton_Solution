# ==========================================
# Environment Variables - Example Template
# ==========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ─── Database ───────────────────────────────
POSTGRES_DB=incident_platform
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# ─── Grafana ────────────────────────────────
GRAFANA_USER=admin
GRAFANA_PASSWORD=changeme

# ─── Services ───────────────────────────────
NODE_ENV=development
LOG_LEVEL=info

# ─── Redis ──────────────────────────────────
REDIS_URL=redis://redis:6379

# ─── Prometheus & Alertmanager ──────────────
PROMETHEUS_URL=http://prometheus:9090

# ─── Alert Processing Configuration ─────────
# Maximum retries for failed normalizations
NORMALIZATION_MAX_RETRIES=3

# Delay between retries (milliseconds)
NORMALIZATION_RETRY_DELAY_MS=5000

# ─── Correlation Configuration ──────────────
# Time window for correlating alerts (milliseconds - 5 minutes default)
CORRELATION_TIME_WINDOW_MS=300000

# Similarity threshold for alert correlation (0-1)
CORRELATION_SIMILARITY_THRESHOLD=0.7

# ==========================================
# Notes
# ==========================================
# 1. Never commit the actual .env file to Git
# 2. Keep this .env.example file updated
# 3. Use strong passwords for production
# 4. The actual .env file is ignored by .gitignore

